We need to address the ZitoTV player behavior, but we have to proceed carefully because previous deployment scripts broke the VPS layout and corrupted the working UI. The server is currently running at /opt/gigzito, and the layout was restored manually by copying the correct video-card.tsx from Replit to the server via SCP. The system is now stable again and we have created manual SSH backups under /opt/gigzito-backups, including a full snapshot archive gigzito-working-2026-03-09-2250.tar.gz. That means the UI is currently in a known-good state and we want to preserve it. The issue we need to fix is the ZitoTV player pop-out behavior. When the user clicks the Pop Out button on the ZitoTV player, instead of opening the player it redirects to the registration form and sometimes breaks the page layout. The expected behavior is that Pop Out should simply open the ZitoTV player in a separate window or overlay and should not trigger an authentication redirect or reload the application. One important constraint is that we cannot use bulk deployment scripts again (for example deploy_all.js) because the last time that method overwrote multiple files it broke the entire UI layout on the VPS. Instead we need to identify the exact source file responsible for the ZitoTV pop-out logic and modify only that file. Then we will upload just that single file to the VPS using the same method that successfully restored the layout earlier, for example using SCP to copy the file directly into /opt/gigzito/client/src/.... We suspect the logic may live in one of these files: client/src/pages/zito-tv.tsx, client/src/components/mini-live-player.tsx, client/src/pages/live-view.tsx, or client/src/pages/live-now.tsx, but we need confirmation of which file actually controls the pop-out behavior. We also need to understand why clicking Pop Out is redirecting to the registration page. There may be a guard in the code such as if (!user) redirect('/register') or similar logic that is triggering incorrectly when the pop-out is invoked. The clean fix should allow the player to open without redirecting and without reloading the application. Acceptable implementations would be something like a window.open() call for the player, a modal overlay, or a React portal approach that isolates the player from the authentication routing logic. The goal is a one-file change that fixes the ZitoTV pop-out, does not affect the feed layout, does not require a full redeploy, and can be copied directly to the VPS as a single file replacement. If possible, please provide the exact code block that needs to be modified rather than another multi-file deployment script so we can apply the change safely without risking another layout break.