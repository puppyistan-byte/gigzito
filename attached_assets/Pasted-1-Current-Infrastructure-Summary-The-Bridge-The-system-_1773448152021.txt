1. Current Infrastructure Summary (The Bridge)The system is currently running a decoupled architecture, splitting the application logic from the data storage to ensure high availability and performance.ComponentServer NameIP AddressRoleStatusThe Switchboardubuntu-8gb-ash-25.161.102.88Node.js (PM2), Nginx Reverse Proxy, Frontend UILiveThe Vaultubuntu-8gb-hil-25.78.128.185PostgreSQL 14 (Port 5432), Data StorageLiveConnectivity Details:Authentication: Ashburn connects to Hillsboro via md5 authenticated remote handshake.Network: Hillsboro is configured to only accept incoming Postgres traffic from the Ashburn IP (Whitelist).Process Management: Ashburn uses PM2 to manage the GeeZee-Switchboard lifecycle.2. Technical Requirements: GeeZee Business Card ModuleThis module introduces a revenue-gated feature where Providers purchase and customize digital/physical identity cards.A. Database Schema Expansion (Hillsboro)We need to implement a new business_cards table.User Relation: 1:1 mapping to existing users table.Data Types: * gallery: TEXT[] (Array of up to 6 image URLs).socials: JSONB (Flexible key-value pairs for IG, TikTok, X, etc.).payment_status: BOOLEAN (Triggers the gate for the "Edit" UI).qr_identifier: UUID (A unique string for the QR code to ensure security).B. Application Logic (Ashburn)The "Gate" Middleware: A function to check is_purchased status before allowing POST/PUT requests to the card configuration.Flip-Card Component: A CSS3 3D-transform UI component.Side A (Static): High-resolution QR Code generation using qrcode or canvas libs.Side B (Dynamic): Profile pic (top-right), Username (header), and a 6-item CSS grid for lifestyle photos.Social Landing Page: A new route /card/:username that acts as the target for the QR code. This page must bypass the internal Gigzito profile and link directly to external social handles.C. Asset ManagementStorage: We need to decide if images are stored locally on Ashburn or in an S3-compatible bucket (recommended for 8GB servers to prevent disk bloat from the 6-pic gallery).3. Deployment Checklist for RIPStripe/PayPal Integration: Need API keys for the purchase trigger.Image Optimization: Implementation of sharp or similar to compress the 6-pic galleries automatically.QR Pointing: Confirm the final URL structure (e.g., gzto.me/username) for the QR codes to keep them short and "scannable."