node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
*.tar.gz.env
.env.bak*
.env

# ── Safe Havens — Rocco cannot touch these ───────────────────────────────────
# Uploaded media, ads, and logs: never commit actual files, keep dir structure
uploads/**/*
!uploads/**/.gitkeep
ads/**/*
!ads/.gitkeep
logs/**/*
!logs/.gitkeep
