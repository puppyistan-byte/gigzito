node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
*.tar.gz.env
.env.bak*
.env

# Uploaded media — never commit actual files, but keep the directory structure
uploads/**/*
!uploads/**/.gitkeep
